Place certificate files (PDF/JPG/PNG) in this folder.

Static method:
- Add the file here under `public/certificates/`
- Add an entry in `src/data/certificates.ts` that points to the `/certificates/...` path

